# Federation Quick Card — 2026-07-10

> Load this in 5 seconds. Then go.

## Health (probe once)
```
✅ arifos:8088  ✅ aforge:7071  ✅ aaa:3001
✅ geox:8081    ✅ wealth:18082 ✅ well:18083
```

## Tool map
| You need | Reach for |
|----------|-----------|
| Build, deploy, shell, git | `forge_*` (79 live on :7072, ~12 semantic twins — see REDUNDANCY-FINDING) |
| Judgment, session, seal | `arif_*` (11 kernel verbs) |
| Earth science | `geox_*` (16 tools) |
| Capital / money | `wealth_*` (8 tools) |
| Human readiness | `well_*` (22 tools) |

## Init (every agent, every wake)
```
1. arif_init(mode="triage")  — bind session
2. curl-check 6 organs        — verify reality
3. Read carry_forward.json     — scars, NEVER patterns, drift state
4. Load shadow-diagnostic      — pre-output self-check
5. Load route-least-power      — smallest tool first
6. Go.
```

## Memory
- carry_forward: `/root/.local/share/arifos/carry_forward.json`
- seal chain: `/root/.local/share/arifos/vault999/seal_chain.jsonl` (seq 1-8)
- gate tracker: `/root/.local/share/arifos/gate_fire.jsonl`
- skills: `/root/.agents/skills/` (74 canonical)

## Known issues
- ⚠️ arifOS deploy lag (build ≠ live) — acknowledged
- 🔴 WELL state stale (Apr 2026) — needs `biometric_inject.sh`
- ⚠️ A-FORGE surface audit unreliable; 79 live vs 98 declared — see REDUNDANCY-FINDING
- 🟡 VAULT999 60 historical gaps — sovereign: non-issue
