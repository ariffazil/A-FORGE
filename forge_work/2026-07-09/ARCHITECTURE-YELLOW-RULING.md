# ARCHITECTURE-YELLOW-RULING

> **Authority:** Arif / F13  
> **Date:** 2026-07-09  
> **Status:** YELLOW — architecture approved; deploy lag and tool-schema discipline remain open  
> **Receipt:** T1 truth + promote/HOLD ruling

---

## T1 truth (this machine)

| Layer | Status |
|---|---|
| Python judgment engine @ `arifOS/arifosmcp/runtime/kernel/` | on GitHub `1372af4` |
| A-FORGE hands @ `A-FORGE/src/executor/` | on GitHub `9d2dc29` |
| Quantum | `contracts/quantum.py` only (not `organs/quantum.py`) — already has `backend` / `backend_class` / `evidence_rank` + simulator guard |
| Live `:8088` | was still `40fe403` at last probe → **deploy lag HOLD** (HEAD ≠ live until redeploy) |

## Tightened now

- **Wording:** kernel = constitutional judgment engine; F13 remains sovereign (not replaced by code).
- **A-FORGE hard-fail:** missing `receiptId`, `kernelSignature`, `toolName`, `inputHash`, lease/scope, bounds, lineage → **REFUSED** with `refusalReasons`. CRITICAL + irreversible → refuse auto path.
- **Tests:** 7/7 formal kernel tests pass — stated as **local behavior**, not “system sealed.”
- **`arif_triage`:** VOID as public tool — internal_only; use `arif_init` (`status` / `light` / `init`). Never guess schema.

## GEOX eureka — locked

Language before picture. Visual = sensory. Voice = last.

`OBS → DER → INT → SPEC → challenge → falsify → PROCEED|HOLD|KILL`

That already lives in GEOX README competitive lane; this write-up is the soul of it.

## One-line doctrine

> Keep judgment in Python, hands in A-FORGE, quantum as evidence only, TypeScript as contract only, VAULT999 as lineage, Arif above all code.

## Promote / HOLD / VOID

| Item | Verdict |
|---|---|
| Python kernel as constitutional judgment engine | **PROMOTE** |
| TS A-FORGE as hands | **PROMOTE** |
| Quantum as organ with backend labeling | **PROMOTE** |
| TS shared types only | **PROMOTE** |
| Live `:8088` deploy lag | **HOLD** until HEAD = live |
| Simulated quantum fallback unlabeled | **HOLD** (now labeled in `contracts/quantum.py`) |
| `arif_triage` guessed schema | **VOID** — use `arif_init` |

---

*DITEMPA BUKAN DIBERI*
