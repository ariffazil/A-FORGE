# TODO — A-FORGE Metabolic Shell

> **Last Updated:** 2026-05-10  
> **Session:** Governance Attestation + Execution Hardening  
> **Seal:** DITEMPA BUKAN DIBERI

---

## ✅ Completed This Session

- [x] **arifOS embodiment contracts** deployed — A-FORGE benefits from kernel-level tool gating
- [x] **Model registry** fixed — `gpt-5.5-thinking` resolves for governance attestation

---

## 🔴 P0 — Critical (Before Next Session)

### Merge h1-roadmap Branch
A-FORGE is currently on branch `h1-roadmap-1778019172`, diverged from `main`.

- [ ] **Rebase or merge** `h1-roadmap` into `main`
- [ ] **Resolve conflicts** — likely in `src/engine/AgentEngine.ts` and `src/governance/`
- [ ] **Full test pass** — `npm test` must pass before merge
- [ ] **Update Docker image** — rebuild `af-bridge` after merge

**Blocked by:** Potential breaking changes in governance layer — needs careful review.

### Qdrant DNS Test Errors
Tests pass but log non-fatal `EAI_AGAIN qdrant` errors when Qdrant unreachable.

- [ ] **Mock Qdrant client** in test context — or skip Qdrant-dependent tests when unavailable
- [ ] **Suppress non-fatal errors** in test output to reduce noise
- [ ] **Document test requirements** — which tests need Qdrant vs which are pure unit

---

## 🟠 P1 — High (Next 7 Days)

### Dry-Run Sandboxing Standard (H1.1)
Every execution plan must pass through isolated sandbox before live execution.

- [ ] **SandboxProfile interface** — `reflex` | `tactical` | `strategic` | `sovereign`
- [ ] **Network policies** — null | localhost-only | federation-only | open
- [ ] **Filesystem policies** — none | readonly | scratch-only | full
- [ ] **Tool whitelists** per profile
- [ ] **Wire to arifOS 888_JUDGE** — judge issues sandbox profile with verdict

### Rollback Atomicity (H1.2)
Atomic rollback segments: if execution violates a floor mid-stream, revert to checkpoint.

- [ ] **CheckpointManager** — `createCheckpoint()`, `restoreCheckpoint()`, `listCheckpoints()`
- [ ] **Capture state** — filesystem hashes, memory snapshot, VAULT999 last hash
- [ ] **Trigger conditions** — VOID verdict, SEA-Guard block, anomalous compute, floor violation
- [ ] **CI test** — simulate floor violation mid-execution, verify rollback

### Metabolic Telemetry API (H1.3)
Expose real-time D-M-E loop metrics so arifOS can throttle or kill runaway processes.

- [ ] **D-M-E metrics** — detect/metabolize/execute latency, throughput, memory, compute units
- [ ] **Throttle endpoint** — `POST /api/v1/telemetry/throttle`
- [ ] **Kill signal** — arifOS sends halt to specific workload_id

---

## 🟡 P2 — Medium (Next 30 Days)

### Federation Mesh Boot Sequence (H1.4)
Formalize cold-start order with health checks.

```yaml
boot_sequence:
  - VAULT999 → arifOS → WELL → WEALTH → GEOX → AAA → A-FORGE
  - timeout: 300s
  - failure_action: halt_and_alert
```

- [ ] **Boot order YAML** — `federation_boot_order.yaml`
- [ ] **Health check polling** — each organ waits for dependencies before starting
- [ ] **Auto-restart** — failed organs restart in sequence order
- [ ] **Alert on timeout** — Telegram/OpenClaw notify operator

### Self-Modification Execution (H2.1)
Execute self-modification proposals generated by AAA, judged by arifOS.

- [ ] **Pipeline:** AAA proposes → arifOS F14 judgment → A-FORGE executes → VAULT999 records
- [ ] **Health check after modification** — continue or rollback
- [ ] **BEND rollback path** — reversibility proof for every self-modification

---

## 🟢 P3 — Backlog (H2 2026)

### Energy-Aware Scheduling (H3.2)
- [ ] **Route to low-carbon compute** — temporal (off-peak) and spatial (regional carbon intensity)
- [ ] **Data sources** — electricityMap.org, WEALTH carbon intensity API
- [ ] **Task urgency classification** from arifOS 888_JUDGE

### Disaster Recovery (H3.3)
- [ ] **Any 2 organs may fail; federation continues**
- [ ] **Hot standby** for critical organs
- [ ] **Auto-restart** failed organs in boot sequence order

---

## Dependency Chain

```
[h1-roadmap merge] ──► [Dry-run Sandbox] ──► [Rollback Atomicity]
         │                                    │
         └──────► [Telemetry API] ◄───────────┘
                        │
                        ▼
              [Boot Sequence] ──► [Self-Modification]
```

---

**DITEMPA BUKAN DIBERI — Metabolic execution is forged, not given.**
