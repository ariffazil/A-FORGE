# 👑 WORKFLOW 5: THE GOLDEN PATH
# The One Workflow That Proves the Stack
# Single end-to-end flow touching all 5 layers (L0→L4).
# This is the civilization's heartbeat.

```yaml
workflow_id: wf-golden-path
canonical_name: "THE GOLDEN PATH — Full Stack Traversal"
layers_traversed: [L0, L1, L2, L3, L4]
organs_touched: [ALL 9]
metabolic_steps: [000, 111, 333, 444, 555, 666, 777, 888, 999]
```

## The Path

```
L0 CANON: Arif opens AAA cockpit, declares intent
    "Evaluate Basin X. If viable, compute capital. If I'm tired, pause."
    ↓
L1 ROOT: arifOS arif_init → session bound, floors active
    ↓
L3 DOMAIN: A-FORGE spawns 3 agents (parallel)
    geo_agent    → GEOX:  geox_basin + geox_prospect
    health_agent → WELL:  well_validate_vitality
    risk_agent   → WEALTH: capital_diagnose(mode="stress_index")
    ↓
L1 ROOT: arifOS arif_think(mode="plan")
    "Basin X: POS=0.35, EMV=$120M. Arif vitality: OPTIMAL. Risk: MODERATE."
    ↓
L1 ROOT: arifOS arif_judge
    F1 (AMANAH): Reversible? Yes (evaluation only)
    F2 (TRUTH): Evidence labeled OBS/DER/INT
    F3 (WITNESS): W³ = ∛(human×ai×external) = 0.82 ≥ 0.75
    F7 (HUMILITY): Ω₀ = 0.04 ∈ [0.03, 0.05]
    F8 (GENIUS): G = 0.87 ≥ 0.80
    F9 (ANTI-HANTU): C_dark = 0.12 < 0.30
    → VERDICT: SEAL
    ↓
L2 EXECUTIVE: A-FORGE forge_execute
    "Generate basin evaluation report → /root/forge_work/YYYY-MM-DD/"
    ↓
L1 ROOT: arifOS arif_seal → VAULT999
    Hash: sha256(entire_evidence_package)
    Chain: prev_seal_id → this_seal_id
    ↓
L4 PUBLIC: arif-sites updates federation dashboard
    "Basin X evaluation: COMPLETE. View report."
    ↓
L0 CANON: Arif sees result in AAA cockpit. Done.
```

## What This Proves

| Layer | Proof |
|-------|-------|
| L0 | Human sovereignty preserved (Arif initiated, Arif sees result) |
| L1 | Constitutional physics enforced (9 floors checked, all passed) |
| L2 | Governed execution (SEAL→execute, no self-authorization) |
| L3 | Domain evidence computed (3 organs, parallel, isolated) |
| L4 | Public surface updated (arif-sites reflects truth) |

## Invariant Checks

- [ ] No organ called another organ directly
- [ ] No execution without prior SEAL
- [ ] No evidence without provenance label
- [ ] No seal without judge
- [ ] No judge without evidence
- [ ] Arif saw the result

## The Golden Path in One Command

```bash
# From /root/A-FORGE
forge_execute \
  --task "Evaluate Basin X end-to-end" \
  --mode "full" \
  --auto_predict true \
  --parallel_agents 3
```

When this runs clean — the civilization stack is alive.
