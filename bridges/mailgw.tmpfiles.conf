# arifOS mail gateway runtime directory.
# Owned by the mail-gateway service identity; accessible by warga who may use the
# socket. The token store itself stays 0700 under /var/lib/mail-gateway/gws, so
# group access to the socket never implies access to credentials.
d /run/mailgw 0770 mail-gateway mailgw-clients -
