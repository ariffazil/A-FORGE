# RBG AUDIT — A-FORGE MCP Tools · Survival of the Fittest
**Date:** 2026-09-04 · **Mandate:** F13 sovereign directive "audit all aforge mcp tools and apply rbg test and survival of the fittest tool"
**Agent:** kimi-code/FI-008 · **Session:** SEAL-4de43d3c1798471e (kernel band: OBSERVE_ONLY; mutations via FI-seat 333-AGI continuous-execution authority, git-reversible)
**RBG Law:** REMOVE BENDA GILA (`/root/AAA/docs/ZEN_888_FULL.md`): one tool one surface · no orphan capability · organ owner nameable in 10s
**Fitness Law:** `F = (Value × Adoption) / (Entropy × BlastRadius × CognitiveCost)` (`skills/tool-fitness-compiler/`, fiqh: wajib≥0.6 / sunat≥0.4 / harus≥0.2 / makruh≥0.1 / haram<0.1)

---

## 1. Ground truth (two independent sources, name-for-name)

- **Live surface: 118 tools** — (a) direct `tools/list` from MCP :7072 (this session, curl), (b) subagent static analysis: all `server.tool()/registerTool()` sites in `src/` ≡ `.registry/fingerprints.json` (written at boot from `server._registeredTools`).
- Registration hub: `src/interfaces/mcp/core.ts:131` (serve.ts = transport-only).
- **After this audit: `a_think/affordances.yaml` = 118 cards == live 118. Zero drift both directions.**

## 2. Actions executed (all reversible; rollback = `git revert`)

| # | Action | Evidence |
|---|--------|----------|
| A1 | Removed 5 dead affordance cards: `forge_telegram_send`, `forge_calendar`, `forge_drive`, `forge_gmail`, `forge_sheets` | Tools absent from live 118. GWS four swept off surface 2026-08-17 (core.ts:3096, commit 21e8a608c "Phase 2 ZEN Sweep") but cards lingered; telegram_send never existed in src/dist — its card claimed `card_status: registered / card_state: DEPLOYED` (REALITY_LEAK). |
| A2 | Removed 2 dual-era proxy cards: `forge_whoami`, `forge_proxy_call` | Not on live surface; orphaned `serveModern.ts` era leftovers. |
| A3 | Added missing card for `auth_pipeline` (live since core.ts:3066) | Live tool was undocumented — audit gap (see §4b). |
| A4 | Refreshed header note (provenance of removal + rollback path) | — |

Net: `a_think/affordances.yaml` −136/+22 lines, YAML parses, **119→118 cards, exact live parity**.

## 3. Fitness verdicts (post-cleanup run, 119 scored incl. 1 pre-cleanup artifact → 118 live)

**Distribution:** wajib 1 · sunat 76 · harus 34 · makruh 8 · haram 0. Mean healthy. **Zero haram. No code-level kills warranted** — all 118 map to 14 actively-maintained source files (none stale >60d).

**Makruh-8 — all SURVIVE on constitutional override** (fitness heuristic measures adoption, not load-bearing-ness; F13 pins > fitness):

| Tool | F | Why it survives |
|------|---|-----------------|
| forge_execute | 0.154 | PINNED required (config/mcp-surface-guard.json); EXECUTE stage of AUTH pipeline |
| forge_docker | 0.154 | PINNED required; canonical docker primitive |
| forge_pipeline_run | 0.154 | AUTH pipeline runner (execute lane) |
| forge_abort | 0.154 | P2.3 safe-stop+rollback — safety tool; zero usage is GOOD |
| forge_send_confirm / forge_transfer_confirm | 0.154 | F13 consent elicitation gates — exist to block, not to be used |
| forge_browser_click / forge_browser_type | 0.189 | complementary browser pair (rare automation) |

**FIT-HOT (≥5 calls/30d via gateway):** forge_probe(15) forge_shell(12) forge_fetch(7) forge_vps_ports(5). Only 22 of 118 tools saw any gateway traffic in 30d — **adoption is the weakest fitness axis federation-wide** (agents mostly reach A-FORGE via stdio seats, gateway log is one signal, not the whole).

## 4. Bugs & gaps found (tickets)

- **BUG-1 (tool): `forge_surface_audit` reads a stale registry artifact** — post-cleanup it reports 4 FALSE drifts: calls `auth_pipeline`+`forge_gemini` phantoms (both provably live) and `forge_proxy_call`+`forge_whoami` "missing from affordances" (both now in neither). Its registry source ≈ pre-Aug-25 "114 tools" era. Fix: source from `server._registeredTools`/fingerprints.json (verified accurate). Until fixed: ignore its findings, trust this reconciliation.
- **BUG-2 (tool): one-directional phantom check** — never flagged live-without-card (auth_pipeline) until card existed; needs bidirectional diff + fresh source.
- **F4 drift:** `serve.ts` STATELESS_TOOLS whitelist still names 4 unregistered GWS tools (lines 273-276); `serveModern.ts` = dead code ("114 tools"). Cleanup needs src→build→redeploy cycle — next session.
- **RISK: 10 UNCL-IRR tools** (auth_pipeline, forge_apex_*×5, forge_trust_score, forge_compose, forge_minimax_search, forge_hf_import) are absent from `actionClassifier` sets → classify fail-closed IRREVERSIBLE (actionClassifier.ts:417) — possibly policy-gate-unreachable. Needs runtime tools/call probe per tool.
- **Test coverage: 88/118 (74.6%) tools have zero name-level test references** (forge_session_init, forge8 verb family, all apex, entropy trio among them).
- `forge_minimax_search`: comment says "REMOVED 2026-07-31" yet still registered (gatewayTools.ts:1111) — intent conflict; also in MERGE list.
- `forge_postgres` registers DISABLED stub when DATABASE_URL unset (proxyTools.ts:661) — env-dependent surface.

## 5. Orphan fleet — HOLD for 888 (one word: "stop bridges" / "keep")

8 APA bridge services run for tools that are unreachable from the agent surface (GWS unmounted Aug-17; Python APA not bridged by TS server at all — no code touches :18093-18095). Traffic census 10d:

| Service | Port | HTTP calls/10d | Verdict rec |
|---|---|---|---|
| apa-gemini-bridge | 18096 | 4 | **KEEP** — forge_gemini is live |
| apa-github-bridge | 18095 | 1 | keep (1 live caller) or fold into forge_github |
| apa-calendar/drive/email/gmail/sheets/gws | 18092-18099 | **0 each** | **STOP** (`systemctl disable --now`, reversible) |
| apa-telegram-bridge | — | already inactive | nothing to do |

~6 python processes ≈ 80MB RAM for zero function. Sovereign Google OAuth services → not stopped autonomously.

## 6. Survival-of-the-fittest backlog (MERGE/ROUTE debts — separate refactor sessions)

1. **research-stack (MERGE):** forge_minimax_search + forge_docsgpt + forge_docs_lookup → retire into forge_search/forge_fetch/forge_research spine.
2. **github-granular (MERGE):** create_issue / create_or_update_file / get_file → modes of forge_github.
3. **cooling-duo (MERGE):** forge_cool_pattern → optional recurrence params on forge_cool_drift.
4. **git-commit-wrapper (MERGE):** forge_git_commit → forge_git(commit) canonical.
5. **surface_guard ∩ surface_audit (MERGE-LATER):** unify drift modes (recent work, defer).
6. **ROUTE:** forge_sandbox_auto_evict → systemd timer; forge_wealth + forge_well organ bridges → deprecate after arif_route migration (ZEN: A-FORGE = execution only).
7. **HOLD-DESIGN:** forge_visual_seal VAULT999 composite seal vs arif_seal Lane A — separation-of-powers review.
8. Cards of removed GWS tools claim "FFF survival verdict 2026-08-25: RESURRECTED" — that verdict was never executed in code; recorded here so history is not lost.

## 7. Artifacts

- `audit/rbg_scorecard.tsv` — 118 rows: fitness/fiqh/usage/last-commit/family/verdict
- `audit/rbg_fitness_2026-09-04.json` — compiler output (clean surface)
- Evidence chain: gateway journal counts, ArifSeal ledger (57 records, chain valid), VAULT999 receipts, subagent static map (session log)

*DITEMPA BUKAN DIBERI — runtime menang atas dokumentasi.*
